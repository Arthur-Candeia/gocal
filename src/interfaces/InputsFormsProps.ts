interface InputsFormsProps {
  age: number,
  weight: number,
  height: number,
  handleAgeChange: (input: string) => void,
  handleWeightChange: (input: string) => void,
  handleHeightChange: (input: string) => void
}